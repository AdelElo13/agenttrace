{
  "name": "agenttrace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/dashboard",
    "lint": "tsc --noEmit --project packages/core/tsconfig.json && tsc --noEmit --project packages/sdk/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
